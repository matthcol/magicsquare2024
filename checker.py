import numpy as np

def is_magic(square: np.ndarray) -> bool:
    return False

# n (n² + 1) / 2
def magic_sum(n: int) -> int:
    return n * (n**2 + 1) // 2

def is_magic_rows(square: np.ndarray, n: int, ms: int) -> bool:
    pass

def is_magic_columns(square: np.ndarray, n: int, ms: int) -> bool:
    pass

def is_magic_diagonals(square: np.ndarray, n: int, ms: int) -> bool:
    pass

def all_numbers_used(square: np.ndarray, n: int) -> bool:
    pass